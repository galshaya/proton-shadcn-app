"use client";

export default function MetaversePappi() {
  return (
    <div>
      <iframe
        src="https://proto-tech-club-viz.netlify.app/"
        className="w-full h-screen border-0"
        title="Metaverse Pappi"
        allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
        allowFullScreen
      />
    </div>
  );
}
